import { createFileRoute } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { ProductCard } from "@/components/product-card";
import { listProducts } from "@/lib/products.functions";
export const Route = createFileRoute("/shop")({ head: () => ({ meta: [{ title: "Shop Streetwear — Den Mond" }, { name: "description", content: "Browse Den Mond Anstreben tees and tracksuits." }, { property: "og:title", content: "Shop Den Mond Anstreben" }, { property: "og:description", content: "Statement streetwear from Matsulu." }] }), component: Shop });
function Shop() { const { data = [], isLoading } = useQuery({ queryKey: ["products"], queryFn: () => listProducts() }); return <div className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><p className="text-sm font-black uppercase tracking-[.3em] text-primary">The collection</p><h1 className="font-display mt-2 text-5xl font-black uppercase sm:text-7xl">Shop all</h1>{isLoading ? <p className="mt-12">Loading the collection…</p> : data.length ? <div className="mt-10 grid grid-cols-2 gap-x-4 gap-y-10 md:grid-cols-3 md:gap-8">{data.map((product) => <ProductCard key={product.id} product={product}/>)}</div> : <div className="mt-12 border-y border-border py-14"><h2 className="font-display text-2xl uppercase">The next drop is being prepared.</h2><p className="mt-2 text-muted-foreground">Check back soon or message us on WhatsApp for availability.</p></div>}</div>; }